# QA notes — 25 August 2026

The browser preview loaded the full interactive application successfully. The initial Per Person × Loss / GDP evidence board rendered ten country cases, with Tonga selected by default. The **Global share** control was exercised and correctly changed the horizontal axis to “share of global GHG emissions (%)” while retaining the impact measure and country case file.

The preview interface also exposes both burden-mode controls, the ten bubble click targets, the country selector, the country-ranking list, the source anchor, the annual burden time series, and the official Pacific Data Hub SST context chart. The project type check completed successfully after implementation.

The temporary preview is not a public submission URL. A project checkpoint must be saved, after which the user must use the management interface’s **Publish** action to create the public URL required for an interactive Challenge entry.
